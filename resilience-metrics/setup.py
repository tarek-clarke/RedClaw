from setuptools import setup, find_packages

setup(
    name="resilience-metrics",
    version="1.0.0",
    description="Established system resilience scoring metrics for TKDE paper evaluation",
    author="Academic Research",
    packages=find_packages(),
    python_requires=">=3.10",
    install_packages=[],
)
